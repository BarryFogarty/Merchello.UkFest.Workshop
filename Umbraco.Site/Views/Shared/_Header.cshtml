@inherits Umbraco.Web.Mvc.UmbracoViewPage

<div class="row">
    <div class="box text-center">
        <div class="row">
            <div class="col-sm-10 col-sm-offset-1">
                <h1>@Model.GetPropertyValue("headTitle")</h1>
                <div class="text-muted">@Model.GetPropertyValue("headDescription")</div>
            </div>
        </div>
    </div>
</div>
